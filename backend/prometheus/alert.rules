groups:
- name: backend_alerts
  rules:
  - alert: BackendDown
    expr: up{job="smart-waste-backend"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "Backend is down"
      description: "Smart Waste Backend API has been down for more than 1 minute."
